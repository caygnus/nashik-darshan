// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/omkar273/nashikdarshan/ent/event"
	"github.com/omkar273/nashikdarshan/ent/eventexception"
)

// EventException is the model entity for the EventException schema.
type EventException struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// EventID holds the value of the "event_id" field.
	EventID string `json:"event_id,omitempty"`
	// Start of the occurrence being cancelled
	OccurrenceStartAt time.Time `json:"occurrence_start_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EventExceptionQuery when eager-loading is set.
	Edges        EventExceptionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// EventExceptionEdges holds the relations/edges for other nodes in the graph.
type EventExceptionEdges struct {
	// Event holds the value of the event edge.
	Event *Event `json:"event,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// EventOrErr returns the Event value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EventExceptionEdges) EventOrErr() (*Event, error) {
	if e.Event != nil {
		return e.Event, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: event.Label}
	}
	return nil, &NotLoadedError{edge: "event"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EventException) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case eventexception.FieldID, eventexception.FieldEventID:
			values[i] = new(sql.NullString)
		case eventexception.FieldOccurrenceStartAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EventException fields.
func (_m *EventException) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case eventexception.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case eventexception.FieldEventID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_id", values[i])
			} else if value.Valid {
				_m.EventID = value.String
			}
		case eventexception.FieldOccurrenceStartAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field occurrence_start_at", values[i])
			} else if value.Valid {
				_m.OccurrenceStartAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EventException.
// This includes values selected through modifiers, order, etc.
func (_m *EventException) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryEvent queries the "event" edge of the EventException entity.
func (_m *EventException) QueryEvent() *EventQuery {
	return NewEventExceptionClient(_m.config).QueryEvent(_m)
}

// Update returns a builder for updating this EventException.
// Note that you need to call EventException.Unwrap() before calling this method if this EventException
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *EventException) Update() *EventExceptionUpdateOne {
	return NewEventExceptionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the EventException entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *EventException) Unwrap() *EventException {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: EventException is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *EventException) String() string {
	var builder strings.Builder
	builder.WriteString("EventException(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("event_id=")
	builder.WriteString(_m.EventID)
	builder.WriteString(", ")
	builder.WriteString("occurrence_start_at=")
	builder.WriteString(_m.OccurrenceStartAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// EventExceptions is a parsable slice of EventException.
type EventExceptions []*EventException
